build_rules:
  - id: non-high-vuln
    finding_types:
      - vuln
      - secret
    severity:
      - moderate
      - critical
  - id: only_reachable
    severity: [critical, moderate]
    finding_types: [oss_vuln]
    options:
      reachable: true
